PORT=3000
GEMINI_API_KEY=replace_with_local_key
GEMINI_MODEL=gemini-2.5-pro
# Render currently forwards through one trusted proxy. Keep 0 for direct hosting.
TRUST_PROXY_HOPS=1
# Mobile clients do not need CORS. Add comma-separated browser origins only when used.
ALLOWED_ORIGINS=
